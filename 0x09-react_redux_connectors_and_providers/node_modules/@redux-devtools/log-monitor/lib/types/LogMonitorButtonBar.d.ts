import React, { PureComponent } from 'react';
import { LiftedAction } from '@redux-devtools/core';
import type { Base16Theme } from 'react-base16-styling';
import { Action, Dispatch } from 'redux';
import { LogMonitorAction } from './actions';
import { LogMonitorState } from './reducers';
interface Props<S, A extends Action<string>> {
    theme: Base16Theme;
    dispatch: Dispatch<LogMonitorAction | LiftedAction<S, A, LogMonitorState>>;
    hasStates: boolean;
    hasSkippedActions: boolean;
}
export default class LogMonitorButtonBar<S, A extends Action<string>> extends PureComponent<Props<S, A>> {
    handleRollback: () => void;
    handleSweep: () => void;
    handleCommit: () => void;
    handleReset: () => void;
    render(): React.JSX.Element;
}
export {};
