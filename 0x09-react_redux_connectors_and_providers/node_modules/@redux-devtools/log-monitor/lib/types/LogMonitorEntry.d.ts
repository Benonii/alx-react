import React, { MouseEventHandler, PureComponent } from 'react';
import type { ShouldExpandNodeInitially } from 'react-json-tree';
import type { Base16Theme } from 'react-base16-styling';
import { Action } from 'redux';
interface Props<S, A extends Action<string>> {
    theme: Base16Theme;
    select: (state: any) => unknown;
    action: A;
    actionId: number;
    state: S;
    previousState: S | undefined;
    collapsed: boolean;
    inFuture: boolean;
    selected: boolean;
    error: string | undefined;
    expandActionRoot: boolean;
    expandStateRoot: boolean;
    markStateDiff: boolean;
    onActionClick: (id: number) => void;
    onActionShiftClick: (id: number) => void;
}
export default class LogMonitorEntry<S, A extends Action<string>> extends PureComponent<Props<S, A>> {
    printState(state: S, error: string | undefined): React.JSX.Element;
    handleActionClick: MouseEventHandler<HTMLDivElement>;
    shouldExpandNodeInitially: ShouldExpandNodeInitially;
    render(): React.JSX.Element;
}
export {};
